создай программу 
Проект: Десктопная программа форензик-/античит-сканера
Язык реализации: C++ (Windows)
Цель: Локальный анализ системы пользователя на наличие признаков использования читов / запрещённых модификаций.

1. Общие требования

Программа разрабатывается на C++ под Windows (x64). 

Приложение должно иметь:

консольный/текстовый лог;

графический интерфейс с индикатором прогресса и итоговой «оценкой риска» в процентах;

отдельные визуальные блоки логов:

Лог хешей;

Лог браузера;

Лог Discord.

В рамках проекта разработчик должен предоставить:

полную структуру проекта (модули, классы, файлы);

полный исходный код;

краткую инструкцию по сборке.

2. Основные задачи программы

Анализ хешей файлов:

Поддержка собственной базы хешей (файл/файлы с известными хешами запрещённого ПО и читов).

Подсчёт хешей (SHA-256 или другой криптостойкий алгоритм) файлов в заданных директориях.

Сопоставление с базой и логирование совпадений / подозрений.

Проверка системных директорий:

Сканирование папок:

%APPDATA%\Microsoft\Windows\Recent

C:\Windows\Prefetch

Для найденных файлов:

подсчёт хеша;

при необходимости — дополнительный анализ имени/маски файла.

Проверка активности в Discord:

Анализ состояния пользователя в Discord-каналах, связанных с:

читами,

запрещёнными модами,

сообществами по обходу античита.

Возможные реализации (на выбор разработчика, согласно выбранной архитектуре):

анализ локальных файлов Discord (логи, leveldb, кеши) на наличие ключевых слов и названий каналов/серверов;

либо работа с заранее экспортированными логами Discord (txt/json/html).

Проверка посещённых сайтов / авторизаций:

Анализ данных посещения/авторизации на конкретных сайтах, связанных с читами:

проверка по заранее заданному whitelist/blacklist урлов/доменов;

анализ истории/логов браузера или экспорта My Activity.

Детект:

факта посещения сайта;

попыток авторизации (если по данным возможно однозначно определить).

Проверка My Activity / истории браузеров:

Анализ файлов экспорта (например, My Activity в формате JSON/HTML или логи браузеров) на наличие ключевых слов:

чит

cheat

vanish

Возможность легко добавить новые ключевые слова через конфиг / файл.

3. Визуальное оформление (UI/UX)
3.1. Главный экран

Элементы главного окна:

Индикатор шкалы процентов (общий риск):

Прогресс-бар / индикатор, показывающий общую оценку риска (0–100%).

Цветовая индикация:

Зелёный 🟢: 0–20%

Жёлтый 💛: 30–50%

Красный 🔴: >70%

Значения между зонами (например, 20–30% и 50–70%) можно отнести к промежуточным состояниям (на усмотрение разработчика: либо ближе к соседней зоне, либо нейтральный цвет).

Общий лог / сводка:

Краткая текстовая панель с резюме:

количество сработавших триггеров по хешам;

количество подозрительных совпадений по браузеру;

количество триггеров по Discord;

итоговый процент риска.

Подробные логи в отдельной структуре:

Три независимых блока (например, три вкладки или три раскрывающихся панели):

Лог хешей

Лог браузера

Лог Discord

Каждый блок содержит:

список событий/триггеров;

возможность раскрыть каждую запись (expand/collapse) для детального просмотра.

3.2. Детальный просмотр записи лога

Для каждой записи при раскрытии должны отображаться:

Тип триггера (хеш/ключевое слово/URL/название сервера и т.п.).

Источник:

путь к файлу;

для браузера — тип/название браузера или блок логов;

для Discord — имя канала/сервера (если доступно) или путь к файлу, где найдено совпадение.

Совпавшее значение:

найденный хеш / совпавший с базой;

найденное ключевое слово (чит, cheat, vanish, и т.п.);

конкретный URL / домен.

Оценка «силы» триггера в процентах (локальный вес).

Время обнаружения (штамп времени сканирования).

4. Логика оценки риска (проценты и цвета)
4.1. Общий принцип

Каждому типу события присвоить вес (коэффициент риска), например:

Совпадение хеша с базой запрещённого софта: высокий вес (например, +30–50%).

Найденные ключевые слова в My Activity/бразуере, связанные с поиском читов: средний вес (например, +10–20%).

Активность/участие в Discord-каналах по читам: высокий или средний вес (зависит от контекста).

Общая оценка риска:

суммировать веса событий;

ограничить результат диапазоном 0–100%;

отобразить в прогресс-баре и цветовой шкале.

4.2. Цветовая шкала

0–20% — зона Зелёный 🟢: низкий риск, ничего критичного не обнаружено.

30–50% — зона Жёлтый 💛: присутствуют подозрительные признаки, рекомендуется дополнительная проверка.

>70% — зона Красный 🔴: высокая вероятность наличия читов/запрещённых модов.

(Интервалы 20–30 и 50–70% можно трактовать как переходные — уточнить в реализации.)

5. Функциональные требования по модулям
5.1. Модуль анализа хешей

Задачи:

Загрузка локальной базы хешей:

формат: текстовый файл/JSON (на усмотрение разработчика);

поддержка нескольких категорий (например, cheats, modmenu, injectors и т.п.).

Сканирование файлов:

Директории:

%APPDATA%\Microsoft\Windows\Recent

C:\Windows\Prefetch

дополнительные директории, задаваемые в конфиге.

Подсчёт хеша:

Алгоритм: SHA-256 (предпочтительно) или другой криптостойкий.

Сравнение с базой.

Логирование:

файл, имя;

посчитанный хеш;

совпадение/не совпадение;

категория совпадения (если есть).

5.2. Модуль анализа браузера / My Activity

Задачи:

Работа с файлами истории/экспорта:

My Activity (JSON/HTML/другой формат);

история браузеров (при наличии формата/доступа);

пути к файлам задаются в конфиге или через интерфейс.

Поиск по ключевым словам:

минимум: чит, cheat, vanish;

возможность расширять список через конфигурационный файл.

Поиск по списку сайтов:

конфигурационный файл с перечнем доменов/URL, связанных с читами и запрещёнными сайтами.

детект:

факт посещения url/домена;

при возможности — попытка авторизации (по наличию форм «login/auth/sign-in», куки и т.п. — если это реализуемо в рамках формата данных).

Логирование:

найденные совпадения по словам;

найденные совпадения по сайтам;

привязка к источнику (файл/тип браузера).

5.3. Модуль анализа Discord

Задачи:

Анализ источников данных:

локальные файлы Discord (логи, leveldb, кеш);

либо заранее экспортированные чаты/логи (txt, json, html).

Поиск:

по ключевым словам (чит, cheat, vanish и прочие, заданные в конфиге);

по названиям серверов/каналов, которые заранее известны как связанные с читами (список в конфиге).

Результат:

список найденных совпадений;

указание сервера/канала (если возможно извлечь);

оценка риска по каждому совпадению.

Логирование:

для каждого триггера:

файл-источник;

тип совпадения (слово, название канала/сервера);

контекст (часть строки/сообщения при наличии).

6. Архитектура и структура проекта

Рекомендуемая (примерная) структура:

src/

main.cpp — точка входа, инициализация, запуск сканирования и UI.

Config.h / Config.cpp — работа с конфигурацией (пути, ключевые слова, списки сайтов, веса).

Logger.h / Logger.cpp — логирование (в файл и в память для UI).

HashScanner.h / HashScanner.cpp — работа с хешами и базой хешей.

BrowserScanner.h / BrowserScanner.cpp — анализ My Activity / истории браузера.

DiscordScanner.h / DiscordScanner.cpp — анализ Discord логов/данных.

RiskCalculator.h / RiskCalculator.cpp — подсчёт итогового процента риска.

UI/ — реализация графического интерфейса:

MainWindow.* — главное окно, прогресс-бар, вкладки логов.

Конкретный UI-фреймворк (Qt / WinAPI / ImGui / др.) выбирается разработчиком, но должен обеспечивать:

прогресс-бар с цветами;

вкладки или collapsible панели;

список логов с раскрывающимися элементами.

7. Нефункциональные требования

Производительность:

Приложение должно корректно работать на среднестатистическом ПК, не «подвешивая» систему при стандартных объёмах логов и истории.

При сканировании больших директорий/файлов — отображать прогресс.

Безопасность:

Программа не должна модифицировать или удалять пользовательские файлы.

Только чтение и анализ, запись — только в собственные лог-файлы.

Конфигурируемость:

Все списки (пути, ключевые слова, сайты, хеши, веса) должны быть вынесены в конфигурационные файлы или легко редактируемые структуры.

Локализация:

Интерфейс и логи — на русском языке.

Внутри возможно использование английских терминов, если это необходимо.

8. Выходные данные и отчётность

Файловый лог:

Единый файл (scan_log.txt или аналог), в котором хранится текстовый лог всего сканирования.

Внутренний лог для UI:

Структура в памяти, позволяющая:

отображать списки событий по категориям (хеши/браузер/Discord);

раскрывать подробные записи по отдельности.

Итоговые показатели (для отображения и возможной интеграции):

общий процент риска (0–100);

количество триггеров по каждой категории;

цветовая оценка (зелёный/жёлтый/красный).

9. Финальный результат от разработчика

Полный исходный код (структурированный по модулям, с комментариями).

Инструкция по сборке

Исполняемый файл (релизная сборка) для тестов.

При необходимости — краткая документация по конфигурации:

формат базы хешей;

формат конфигов (пути, ключевые слова, сайты, веса);

как добавлять новые правила.
