# Форензик-сканер / Античит-сканер

Десктопная программа для локального анализа системы на наличие признаков использования читов и запрещённых модификаций.

## 📋 Описание

Программа выполняет комплексный анализ системы пользователя:
- **Анализ хешей файлов** - сканирование системных директорий и сравнение с базой известных читов
- **Анализ браузера** - проверка истории посещений и поиск подозрительных сайтов/ключевых слов
- **Анализ Discord** - поиск активности в каналах и серверах, связанных с читами

## 🖥️ Системные требования

- Windows 10/11 x64
- Visual Studio 2019 или новее (с поддержкой C++17)
- CMake 3.16 или новее
- DirectX 11

## 📁 Структура проекта

```
forensic-scanner/
├── src/
│   ├── main.cpp              # Точка входа
│   ├── Types.h               # Общие типы данных
│   ├── Utils.h/.cpp          # Утилиты (хеширование, работа с файлами)
│   ├── Config.h/.cpp         # Загрузка конфигурации
│   ├── Logger.h/.cpp         # Система логирования
│   ├── HashScanner.h/.cpp    # Модуль анализа хешей
│   ├── BrowserScanner.h/.cpp # Модуль анализа браузера
│   ├── DiscordScanner.h/.cpp # Модуль анализа Discord
│   ├── RiskCalculator.h/.cpp # Расчёт уровня риска
│   └── UI/
│       ├── MainWindow.h/.cpp # Графический интерфейс (ImGui)
├── config/
│   ├── keywords.txt          # Ключевые слова для поиска
│   ├── blacklist_sites.txt   # Чёрный список сайтов
│   ├── discord_servers.txt   # Подозрительные Discord сервера
│   ├── hash_database.txt     # База хешей запрещённого ПО
│   ├── scan_paths.txt        # Дополнительные пути сканирования
│   └── weights.txt           # Веса триггеров
├── libs/
│   └── imgui/                # Библиотека Dear ImGui (скачать отдельно)
├── build/                    # Директория сборки
├── CMakeLists.txt            # Файл сборки CMake
└── README.md                 # Этот файл
```

## 🔧 Инструкция по сборке

### 1. Установка зависимостей

1. Установите [Visual Studio 2019/2022](https://visualstudio.microsoft.com/) с компонентами:
   - "Разработка классических приложений на C++"
   - Windows 10/11 SDK

2. Установите [CMake](https://cmake.org/download/) (версия 3.16+)

3. Скачайте [Dear ImGui](https://github.com/ocornut/imgui):
   ```bash
   cd forensic-scanner/libs
   git clone https://github.com/ocornut/imgui.git
   ```

### 2. Сборка проекта

**Через командную строку (Developer Command Prompt):**

```bash
cd forensic-scanner
mkdir build
cd build
cmake .. -G "Visual Studio 17 2022" -A x64
cmake --build . --config Release
```

**Через Visual Studio:**

1. Откройте папку `forensic-scanner` в Visual Studio
2. Дождитесь генерации CMake кеша
3. Выберите конфигурацию Release
4. Сборка → Собрать всё

### 3. Запуск

После сборки исполняемый файл находится в:
```
build/bin/Release/ForensicScanner.exe
```

Убедитесь, что папка `config/` находится рядом с exe-файлом.

## ⚙️ Конфигурация

### keywords.txt
Ключевые слова для поиска (по одному на строку):
```
cheat
hack
aimbot
чит
хак
```

### blacklist_sites.txt
Домены подозрительных сайтов:
```
unknowncheats.me
mpgh.net
wemod.com
```

### hash_database.txt
База хешей запрещённого ПО:
```
# Формат: hash|category|name|weight
a1b2c3d4...|cheats|CheatEngine|45
```

### discord_servers.txt
Подозрительные Discord сервера и каналы:
```
[servers]
unknowncheats
mpgh

[channels]
cheat-discussion
hack-talk
```

### weights.txt
Веса триггеров (влияние на общий риск):
```
hash_match=40
keyword_found=15
suspicious_url=20
discord_server=30
```

## 🎨 Интерфейс

### Главное окно
- **Левая панель**: Индикатор риска (процент + цветная шкала), кнопки управления, статистика
- **Правая панель**: Вкладки логов (Хеши / Браузер / Discord)

### Цветовая индикация риска
- 🟢 **Зелёный** (0-20%): Низкий риск
- 🟡 **Жёлтый** (21-50%): Средний риск, требует проверки
- 🔴 **Красный** (51-100%): Высокий риск

### Детали триггера
При раскрытии записи отображаются:
- Тип триггера
- Источник (путь к файлу)
- Совпавшее значение
- Вес триггера
- Время обнаружения

## 📊 Алгоритм расчёта риска

1. Каждому триггеру присваивается базовый вес
2. Применяется формула уменьшающейся отдачи:
   ```
   эффективный_вес = базовый_вес × (1 + log₂(количество_триггеров))
   ```
3. Сумма ограничивается диапазоном 0-100%

## 🔒 Безопасность

- Программа работает **только на чтение**
- Не модифицирует и не удаляет файлы пользователя
- Записывает только собственные логи (scan_log.txt)

## 📝 Выходные файлы

- `scan_log.txt` - Полный лог сканирования
- `report_YYYYMMDD_HHMMSS.txt` - Экспортированный отчёт

## 🛠️ Технологии

- **Язык**: C++17
- **UI**: Dear ImGui + DirectX 11
- **Сборка**: CMake
- **Хеширование**: Windows BCrypt API (SHA-256)

## 📄 Лицензия

Проект предназначен для образовательных и исследовательских целей.

## ⚠️ Примечание

Для полноценной работы необходимо заполнить базу хешей (`hash_database.txt`) актуальными SHA-256 хешами известного запрещённого ПО.
